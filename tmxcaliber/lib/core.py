# Core library functions for tmxcaliber

def filter_down(data: dict, severity: str = "", events: list = [], permissions: list = [], feature_classes: list = [], ids: list = []) -> dict:
    # ... existing logic for filter_down ...
    # This function should now accept parameters directly and not rely on the Namespace object.
    # Return the filtered data as a dictionary.
